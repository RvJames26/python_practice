#Create a program that ask user to input 10 numbers. Display all numbers that don't have duplicate.
for i in range(0,10):
    numbers = float(input("Enter number:"))
    for check_numbers in numbers:
        if numbers != numbers:
            print(numbers)
        if numbers == numbers:
            continue
